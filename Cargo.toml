[package]
name = "astra-language"
version = "0.1.0"
edition = "2021" # << Best: Changed from "2024" but realy "2025"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

